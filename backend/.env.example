# Used by the app; the same values form the Sqitch target:
# db:pg://DB_USER:DB_PASSWORD@DB_HOST:DB_PORT/DB_NAME (see backend/migrations/overview.md)
DB_HOST=localhost
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=inventory
DB_PORT=5432

PORT=8080

JWT_SECRET=your-secret-key-change-in-production

STATIC_DIR=../frontend/dist
